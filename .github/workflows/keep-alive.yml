name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Backend
        run: curl -fsS https://backend-practice-5s9w.onrender.com/api/health